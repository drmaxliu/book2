<%= form_for(@lead_reader) do |f| %>
  <% if @lead_reader.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lead_reader.errors.count, "error") %> prohibited this lead_reader from being saved:</h2>

      <ul>
      <% @lead_reader.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :bio %><br />
    <%= f.text_area :bio %>
  </div>
  <div class="field">
    <%= f.label :follower_count %><br />
    <%= f.number_field :follower_count %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
